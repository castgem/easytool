# Default values for the ToolEasy chart (single nginx static frontend).
replicaCount: 1

image:
  repository: tooleasy
  pullPolicy: IfNotPresent
  tag: latest

imagePullSecrets: []
nameOverride: ''

podAnnotations: {}
podLabels: {}

service:
  type: ClusterIP
  port: 80

# Container listen port (ToolEasy nginx image listens on 8080).
containerPort: 8080

env: []

ingress:
  enabled: false
  className: ''
  annotations: {}
  hosts:
    - host: usetooleasy.com
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []

# Gateway API (optional)
gateway:
  enabled: false
  name: '' # default: release name + "-gateway" suffix
  namespace: '' # to override the namespace
  annotations: {}
  gatewayClassName: '' # required when enabled=true
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same # Allow routes from the same namespace

httpRoute:
  enabled: false
  annotations: {}
  # parentRefs: # Omit entirely to use default (auto-references gateway in same namespace)
  #   - name: "" # default: gateway.name or {release-name}-gateway
  #     namespace: "" # to override the namespace
  #     sectionName: "http"
  hostnames:
    - usetooleasy.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 101
  runAsGroup: 101
  fsGroup: 101

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL
  seccompProfile:
    type: RuntimeDefault

resources: {}

livenessProbe:
  httpGet:
    path: /
    port: http
readinessProbe:
  httpGet:
    path: /
    port: http
