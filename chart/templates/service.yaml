apiVersion: v1
kind: Service
metadata:
  name: {{ include "tooleasy.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tooleasy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tooleasy.selectorLabels" . | nindent 4 }}
