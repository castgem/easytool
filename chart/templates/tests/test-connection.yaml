apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tooleasy.name" . }}-test-connection"
  labels:
    {{- include "tooleasy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['-qO-', 'http://{{ include "tooleasy.name" . }}:{{ .Values.service.port }}/']
  restartPolicy: Never
